// index.js
import { registerRootComponent } from 'expo';
import App from './App';

// This makes sure the app is registered as the root component
// and is compatible with Expo Go and EAS builds
registerRootComponent(App);
